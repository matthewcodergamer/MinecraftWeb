# V14.7 source mirror synchronization

The organized `src/parts/97a..97d` files are synchronized with the canonical root runtime parts so the GitHub Pages loader and generated `game.js` build use the same V14.7 implementation. V14.6 optimization parts remain in the runtime/build order before V14.7.
